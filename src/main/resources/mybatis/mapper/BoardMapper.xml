<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
PUBLIC "-mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.book.store.mapper.BoardMapper">

<select id="maxNum" resultType="int">
	select nvl(max(boardNum),0) from board
</select>

<insert id="insertData" parameterType="com.book.store.dto.BoardDTO">
	insert into board (boardNum,name,email,subject,content,hitCount,created) 
	values(#{boardNum},#{name},#{email},#{subject},#{content},0,sysdate)
</insert>

<select id="getDataCount" parameterType="map" resultType="int">
	select nvl(count(*),0) from board 
	where ${searchKey} like '%' || #{searchValue} || '%'
</select>

<select id="getLists" parameterType="map" resultType="com.book.store.dto.BoardDTO">
	select * from (
	select rownum rnum,data.* from (
	select boardNum,name,subject,hitCount,to_char(created,'YYYY-MM-DD') created 
	from board where ${searchKey} like '%' || #{searchValue} || '%' 
	order by boardNum desc) data) 
<![CDATA[
	where rnum>=#{start} and rnum<=#{end}
]]>
</select>

<update id="updateHitCount" parameterType="int">
	update board set hitCount=hitCount+1 where boardNum=#{boardNum}
</update>

<select id="getReadData" parameterType="int" resultType="com.book.store.dto.BoardDTO">
	select boardNum,name,email,subject,content,hitCount,created from board 
	where boardNum=#{boardNum}
</select>

<update id="updateData" parameterType="com.book.store.dto.BoardDTO">
	update board set name=#{name},email=#{email},subject=#{subject},content=#{content} 
	where boardNum=#{boardNum}
</update>

<delete id="deleteData" parameterType="int">
	delete from board where boardNum=#{boardNum}
</delete>


<select id="preReadData" parameterType="map" resultType="com.book.store.dto.BoardDTO">
	select data.* from (
	select boardNum,subject from board
	where ${searchKey} like '%' || #{searchValue} || '%' 
<![CDATA[
	and (boardNum<#{boardNum})
	order by boardNum desc)data 
	where rownum=1
]]> 
</select>


<select id="nextReadData" parameterType="map" resultType="com.book.store.dto.BoardDTO">
	select data.* from (
	select boardNum,subject from board
	where ${searchKey} like '%' || #{searchValue} || '%' 
<![CDATA[
	and (boardNum>#{boardNum})
	order by boardNum asc)data 
	where rownum=1
]]> 
</select> 


</mapper>